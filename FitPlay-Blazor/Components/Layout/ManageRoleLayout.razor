@inherits LayoutComponentBase
@implements IDisposable

@inject NavigationManager NavigationManager
@inject ApiClient Api

<AuthorizeView Roles="GymAdmin" Context="gymAuth">
    <Authorized>
        @{
            var gaEmail = gymAuth.User.FindFirst("email")?.Value
                          ?? gymAuth.User.Identity?.Name
                          ?? "admin@gym.com";
            var gaName = gymAuth.User.FindFirst("full_name")?.Value
                         ?? gaEmail;
        }
        <div class="ga-shell">
            <aside class="ga-sidebar">
                <div class="ga-brand">
                    <a href="/gym/dashboard" class="ga-logo">FIT<span>PLAY</span></a>
                    <span class="ga-role-badge">GYM</span>
                </div>
                <nav class="ga-nav">
                    <div class="nav-section">
                        <div class="nav-section-label">VISAO GERAL</div>
                        <NavLink href="/gym/dashboard" Match="NavLinkMatch.All" class="nav-item ga-nav-item" ActiveClass="active">
                            <span class="nav-icon">
                                <svg width="16" height="16" viewBox="0 0 18 18" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                    <rect x="1.5" y="1.5" width="6" height="6" rx="1.5"/>
                                    <rect x="10.5" y="1.5" width="6" height="6" rx="1.5"/>
                                    <rect x="10.5" y="10.5" width="6" height="6" rx="1.5"/>
                                    <rect x="1.5" y="10.5" width="6" height="6" rx="1.5"/>
                                </svg>
                            </span>
                            <span class="nav-label">Dashboard</span>
                        </NavLink>
                        <NavLink href="/gym/setup" class="nav-item ga-nav-item" ActiveClass="active">
                            <span class="nav-icon">
                                <svg width="16" height="16" viewBox="0 0 18 18" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                    <path d="M2 7L9 2l7 5v7.5a2 2 0 01-2 2h-10a2 2 0 01-2-2z"/>
                                    <polyline points="7 16.5 7 9 11 9 11 16.5"/>
                                </svg>
                            </span>
                            <span class="nav-label">My Gym</span>
                        </NavLink>
                    </div>
                    <div class="nav-section">
                        <div class="nav-section-label">GESTAO</div>
                        <NavLink href="/gym/locations" class="nav-item ga-nav-item" ActiveClass="active">
                            <span class="nav-icon">
                                <svg width="16" height="16" viewBox="0 0 18 18" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                    <path d="M16 8c0 5.25-7 9-7 9S2 13.25 2 8a7 7 0 1114 0z"/>
                                    <circle cx="9" cy="8" r="2.5"/>
                                </svg>
                            </span>
                            <span class="nav-label">Locations</span>
                        </NavLink>
                        <NavLink href="/gym/rooms" class="nav-item ga-nav-item" ActiveClass="active">
                            <span class="nav-icon">
                                <svg width="16" height="16" viewBox="0 0 18 18" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                    <rect x="2" y="2" width="14" height="14" rx="2"/>
                                    <path d="M2 6h14M6 16V6"/>
                                </svg>
                            </span>
                            <span class="nav-label">Rooms</span>
                        </NavLink>
                        <NavLink href="/gym/trainers" class="nav-item ga-nav-item" ActiveClass="active">
                            <span class="nav-icon">
                                <svg width="16" height="16" viewBox="0 0 18 18" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                    <path d="M13.5 16v-1.5a3.5 3.5 0 0 0-3.5-3.5h-4a3.5 3.5 0 0 0-3.5 3.5V16"/>
                                    <circle cx="7" cy="5" r="3"/>
                                    <path d="M16 16v-1.5a3 3 0 0 0-2.4-2.93"/>
                                    <path d="M12.5 2.57a3 3 0 0 1 0 4.86"/>
                                </svg>
                            </span>
                            <span class="nav-label">Trainers</span>
                            @if (_pendingTrainerCount > 0)
                            {
                                <span class="nav-badge">@_pendingTrainerCount</span>
                            }
                        </NavLink>
                        <NavLink href="/gym/sessions" class="nav-item ga-nav-item" ActiveClass="active">
                            <span class="nav-icon">
                                <svg width="16" height="16" viewBox="0 0 18 18" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                    <path d="M6 1.5v3M12 1.5v3"/>
                                    <rect x="2" y="4" width="14" height="12" rx="2"/>
                                    <path d="M2 8h14"/>
                                </svg>
                            </span>
                            <span class="nav-label">Sessions</span>
                        </NavLink>
                    </div>
                </nav>
                <div class="ga-bottom">
                    <a href="Account/Manage" class="ga-profile-foot ga-profile-foot--link" data-enhanced-nav="false">
                        <span class="ga-avatar">@GetInitials(gaName)</span>
                        <div class="ga-profile-details">
                            <span class="ga-username">@(_gymName ?? gaName)</span>
                            <span class="ga-email">@gaEmail</span>
                            <span class="ga-role-chip">Gym Admin</span>
                        </div>
                    </a>
                    <form action="Account/Logout" method="post" class="ga-logout-form">
                        <AntiforgeryToken />
                        <input type="hidden" name="ReturnUrl" value="/" />
                        <button type="submit" class="ga-logout-btn" title="Sair" aria-label="Sair">
                            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
                                <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/>
                                <polyline points="16 17 21 12 16 7"/>
                                <line x1="21" y1="12" x2="9" y2="12"/>
                            </svg>
                            <span>Sair</span>
                        </button>
                    </form>
                </div>
            </aside>
            <main class="ga-content manage-cyan">
                @Body
            </main>
        </div>
    </Authorized>
</AuthorizeView>

<AuthorizeView Roles="Trainer" Context="trainerAuth">
    <Authorized>
        @{
            var trainerName = trainerAuth.User.FindFirst("full_name")?.Value
                              ?? trainerAuth.User.Identity?.Name
                              ?? "Trainer";
        }
        <header class="top-nav">
            <div class="nav-inner">
                <a href="/trainer/dashboard" class="logo">FIT<span>PLAY</span></a>
                <nav class="nav-links-top">
                    <NavLink href="/trainer/validations">Validations</NavLink>
                    <NavLink href="/trainer/rooms">Rooms</NavLink>
                    <NavLink href="/trainer/gyms">Gyms</NavLink>
                    <NavLink href="/trainer/exercises">Exercises</NavLink>
                    <NavLink href="/trainer/rooms/booked">Booked rooms</NavLink>
                </nav>
                <div class="profile-dropdown">
                    <button class="profile-trigger" type="button">
                        <span class="profile-avatar">@GetInitials(trainerName)</span>
                        <span class="profile-name">@trainerName</span>
                        <svg class="dropdown-caret" width="12" height="12" viewBox="0 0 12 12" fill="none" xmlns="http://www.w3.org/2000/svg">
                            <path d="M2 4L6 8L10 4" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                    </button>
                    <div class="dropdown-menu">
                        <div class="dropdown-menu-inner">
                            <div class="dropdown-section">
                                <a class="dropdown-item" href="Account/Manage" data-enhanced-nav="false">
                                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="8" r="4"/><path d="M4 20c0-4 3.6-7 8-7s8 3 8 7"/></svg>
                                    Profile
                                </a>
                                <a class="dropdown-item" href="trainer/dashboard">
                                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="7" height="7"/><rect x="14" y="3" width="7" height="7"/><rect x="14" y="14" width="7" height="7"/><rect x="3" y="14" width="7" height="7"/></svg>
                                    Dashboard
                                </a>
                                <a class="dropdown-item" href="trainer/validations">
                                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
                                    Validations
                                </a>
                                <a class="dropdown-item" href="trainer/rooms">
                                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="16" rx="2"></rect><line x1="3" y1="10" x2="21" y2="10"></line></svg>
                                    Rooms
                                </a>
                                <a class="dropdown-item" href="trainer/gyms">
                                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>
                                    Gyms
                                </a>
                            </div>
                            <div class="dropdown-divider"></div>
                            <div class="dropdown-section dropdown-logout-section">
                                <form action="Account/Logout" method="post">
                                    <AntiforgeryToken />
                                    <input type="hidden" name="ReturnUrl" value="/" />
                                    <button type="submit" class="btn-logout-green">
                                        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/><polyline points="16 17 21 12 16 7"/><line x1="21" y1="12" x2="9" y2="12"/></svg>
                                        Log out
                                    </button>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </header>
        <main class="trainer-content manage-green">
            @Body
        </main>
    </Authorized>
</AuthorizeView>

<AuthorizeView Roles="User" Context="userAuth">
    <Authorized>
        @{
            var userName = userAuth.User.FindFirst("full_name")?.Value
                           ?? userAuth.User.Identity?.Name
                           ?? "User";
        }
        <header class="top-nav">
            <div class="nav-inner">
                <a href="/user/dashboard" class="logo">FIT<span>PLAY</span></a>
                <nav class="nav-links-top">
                    <NavLink href="/user/trainings">Trainings</NavLink>
                    <NavLink href="/user/log-workout">Log Workout</NavLink>
                    <NavLink href="/user/book-class">Book Class</NavLink>
                    <NavLink href="/user/my-sessions">My Sessions</NavLink>
                    <NavLink href="/user/progress">My Progress</NavLink>
                    <NavLink href="/user/achievements">Achievements</NavLink>
                </nav>
                <div class="profile-dropdown">
                    <button class="profile-trigger" type="button">
                        <span class="profile-avatar">@GetInitials(userName)</span>
                        <span class="profile-name">@userName</span>
                        <svg class="dropdown-caret" width="12" height="12" viewBox="0 0 12 12" fill="none" xmlns="http://www.w3.org/2000/svg">
                            <path d="M2 4L6 8L10 4" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                    </button>
                    <div class="dropdown-menu">
                        <div class="dropdown-menu-inner">
                            <div class="dropdown-section">
                                <a class="dropdown-item" href="Account/Manage" data-enhanced-nav="false">
                                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="8" r="4"/><path d="M4 20c0-4 3.6-7 8-7s8 3 8 7"/></svg>
                                    Profile
                                </a>
                                <a class="dropdown-item" href="user/dashboard">
                                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="7" height="7"/><rect x="14" y="3" width="7" height="7"/><rect x="14" y="14" width="7" height="7"/><rect x="3" y="14" width="7" height="7"/></svg>
                                    Dashboard
                                </a>
                                <a class="dropdown-item" href="user/progress">
                                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></svg>
                                    My Progress
                                </a>
                                <a class="dropdown-item" href="user/achievements">
                                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="8" r="6"/><path d="M8.21 13.89L7 23l5-3 5 3-1.21-9.12"/></svg>
                                    Achievements
                                </a>
                                <a class="dropdown-item" href="membership">
                                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="1" y="4" width="22" height="16" rx="2" ry="2"/><line x1="1" y1="10" x2="23" y2="10"/></svg>
                                    Membership
                                </a>
                            </div>
                            <div class="dropdown-divider"></div>
                            <div class="dropdown-section dropdown-logout-section">
                                <form action="Account/Logout" method="post">
                                    <AntiforgeryToken />
                                    <input type="hidden" name="ReturnUrl" value="/" />
                                    <button type="submit" class="btn-logout-green">
                                        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/><polyline points="16 17 21 12 16 7"/><line x1="21" y1="12" x2="9" y2="12"/></svg>
                                        Log out
                                    </button>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </header>
        <main class="user-content manage-green">
            @Body
        </main>
    </Authorized>
</AuthorizeView>

@code {
    private string? _gymName;
    private int _pendingTrainerCount;

    protected override async Task OnInitializedAsync()
    {
        NavigationManager.LocationChanged += OnLocationChanged;

        try
        {
            var gym = await Api.GetMyGym();
            _gymName = gym?.Name;

            if (gym is not null)
            {
                var links = await Api.GetGymTrainerLinks(gym.Id);
                _pendingTrainerCount = links.Count(l => l.Status == "Pending");
            }
        }
        catch
        {
            // Not a GymAdmin or API unavailable — safe to ignore
        }
    }

    private void OnLocationChanged(object? sender, LocationChangedEventArgs e)
        => StateHasChanged();

    public void Dispose()
        => NavigationManager.LocationChanged -= OnLocationChanged;

    private static string GetInitials(string? name)
    {
        if (string.IsNullOrWhiteSpace(name)) return "?";
        var parts = name.Trim().Split(' ', StringSplitOptions.RemoveEmptyEntries);
        return parts.Length >= 2
            ? $"{parts[0][0]}{parts[^1][0]}".ToUpperInvariant()
            : name[0].ToString().ToUpperInvariant();
    }
}
